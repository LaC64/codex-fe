# Electron PowerShell Prototype

Minimal Chromium terminal host used to validate PowerShell through Windows ConPTY.

## Run

```powershell
.\start.cmd
```

The prototype currently has one PowerShell terminal. It does not yet implement multiple tabs, Codex session metadata, or workspace restoration.
